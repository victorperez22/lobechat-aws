# Data directories (contain runtime data)
data/

# Local deployment state, SSH keys, infra IDs (never commit)
.deploy/

# GSD/agent planning scratch dir (not part of submission)
.planning/

# Downloaded binaries
contrib/

# Environment files with secrets
.env
db/flyway/.env.flyway
db/flyway/.build/

# Python
__pycache__/
*.py[cod]
.venv/
uv.lock

# AWS credentials and keys
aws_credentials.yaml
*.pem

# IDE
.idea/
.vscode/
*.swp
config/ssh/
